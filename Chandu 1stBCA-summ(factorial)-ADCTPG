#include<stdio.h>
int main()
{
    int a[100],i,f,n,s=0;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&a[i]);
        f=1;
        while(a[i]>0){
            f=f*a[i];
            a[i]--;
        }
        s=s+f;
    }
    printf("%d",s);
return 0;
}
