#include<stdio.h>
#include<stdlib.h>
#include<string.h>
main()
{
    char c[100],s;
    int i,l;
    gets(c);
    s=' ';
    l=strlen(c);
    for(i=0;i<l;i++){
        if(c[i]==s){
            printf("\n");
        }
        else{
            printf("%c",c[i]);
        }
    }
}
