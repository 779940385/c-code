#include<stdio.h>
int fact(int);
main()
{
	int n,f;
	printf("enter any value:");
	scanf("%d",&n);
	f=fact(n);
	printf("factorial value=%d",f);
}
int fact(int n)
{
	int f=1;
	while(n>0)
	{
		f=f*n;
		n--;
	}
	return f;
}
